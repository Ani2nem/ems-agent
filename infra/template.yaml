AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: >
  EMS Agent - production-shaped serverless RCM prototype.
  SKELETON (Phase 0): resource wiring + names are fixed so the ASL substitutions and
  Workstream A handlers line up. Workstream C fleshes out IAM least-privilege, S3+CloudFront/OAC,
  SSM params, Budgets alarm, and CORS/throttling.

Parameters:
  StageName:
    Type: String
    Default: prod
  ModelId:
    Type: String
    Default: amazon.nova-micro-v1:0
    Description: Bedrock model id used by all agents in v1 (swappable per-agent later).
  UseBedrock:
    Type: String
    Default: "true"
    AllowedValues: ["true", "false"]
  EnableSesEmail:
    Type: String
    Default: "false"
  EscalationEmail:
    Type: String
    Default: ""
    Description: Verified SES address for escalation notices (used only when EnableSesEmail=true).

Globals:
  Function:
    Runtime: python3.12
    MemorySize: 256
    Timeout: 30
    Tracing: Active                      # X-Ray
    Architectures: [arm64]               # cheaper
    Environment:
      Variables:
        JOBS_TABLE: !Ref JobsTable
        MODEL_ID: !Ref ModelId
        USE_BEDROCK: !Ref UseBedrock
        ENABLE_SES_EMAIL: !Ref EnableSesEmail
        ESCALATION_EMAIL: !Ref EscalationEmail
  Api:
    TracingEnabled: true

Resources:
  # --- Job state + audit trail (on-demand, TTL-expired) ---
  JobsTable:
    Type: AWS::DynamoDB::Table
    Properties:
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - { AttributeName: pk, AttributeType: S }
      KeySchema:
        - { AttributeName: pk, KeyType: HASH }   # pk = jobId; idempotency key stored as item "IDEMPOTENCY#<key>"
      TimeToLiveSpecification: { AttributeName: ttl, Enabled: true }

  # --- API / BFF Lambda (FastAPI + Mangum) behind HTTP API ---
  ApiFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/
      Handler: app.main.handler          # Mangum handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref JobsTable }
        - Statement:
            - Effect: Allow
              Action: ["bedrock:InvokeModel", "bedrock:Converse"]
              Resource: "*"
            - Effect: Allow
              Action: ["states:StartExecution"]
              Resource: !Ref NegotiationStateMachine
      Environment:
        Variables:
          STATE_MACHINE_ARN: !Ref NegotiationStateMachine
      Events:
        Api:
          Type: HttpApi
          Properties: { ApiId: !Ref HttpApi, Path: /api/{proxy+}, Method: ANY }

  HttpApi:
    Type: AWS::Serverless::HttpApi
    Properties:
      StageName: !Ref StageName
      # TODO(C): restrict CorsConfiguration.AllowOrigins to the CloudFront domain
      CorsConfiguration:
        AllowOrigins: ["*"]
        AllowHeaders: ["*"]
        AllowMethods: ["GET", "POST", "OPTIONS"]

  # --- Step Functions task Lambdas (Workstream A implements handlers) ---
  DenyFunction:
    Type: AWS::Serverless::Function
    Properties: { CodeUri: ../backend/, Handler: app.handlers.deny, Policies: [ { DynamoDBCrudPolicy: { TableName: !Ref JobsTable } }, { Statement: [ { Effect: Allow, Action: ["bedrock:InvokeModel", "bedrock:Converse"], Resource: "*" } ] } ] }
  AppealFunction:
    Type: AWS::Serverless::Function
    Properties: { CodeUri: ../backend/, Handler: app.handlers.appeal, Policies: [ { DynamoDBCrudPolicy: { TableName: !Ref JobsTable } }, { Statement: [ { Effect: Allow, Action: ["bedrock:InvokeModel", "bedrock:Converse"], Resource: "*" } ] } ] }
  ReReviewFunction:
    Type: AWS::Serverless::Function
    Properties: { CodeUri: ../backend/, Handler: app.handlers.rereview, Policies: [ { DynamoDBCrudPolicy: { TableName: !Ref JobsTable } }, { Statement: [ { Effect: Allow, Action: ["bedrock:InvokeModel", "bedrock:Converse"], Resource: "*" } ] } ] }
  FinalRulingFunction:
    Type: AWS::Serverless::Function
    Properties: { CodeUri: ../backend/, Handler: app.handlers.final_ruling, Policies: [ { DynamoDBCrudPolicy: { TableName: !Ref JobsTable } }, { Statement: [ { Effect: Allow, Action: ["bedrock:InvokeModel", "bedrock:Converse"], Resource: "*" } ] } ] }
  ResolveFunction:
    Type: AWS::Serverless::Function
    Properties: { CodeUri: ../backend/, Handler: app.handlers.resolve, Policies: [ { DynamoDBCrudPolicy: { TableName: !Ref JobsTable } } ] }
  EscalateFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/
      Handler: app.handlers.escalate
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref JobsTable }
        - Statement: [ { Effect: Allow, Action: ["ses:SendEmail"], Resource: "*" } ]   # used only when ENABLE_SES_EMAIL=true

  # --- The negotiation state machine ---
  NegotiationStateMachine:
    Type: AWS::Serverless::StateMachine
    Properties:
      Type: STANDARD
      Tracing: { Enabled: true }
      DefinitionUri: statemachine/negotiation.asl.json
      DefinitionSubstitutions:
        DenyFunctionArn: !GetAtt DenyFunction.Arn
        AppealFunctionArn: !GetAtt AppealFunction.Arn
        ReReviewFunctionArn: !GetAtt ReReviewFunction.Arn
        FinalRulingFunctionArn: !GetAtt FinalRulingFunction.Arn
        ResolveFunctionArn: !GetAtt ResolveFunction.Arn
        EscalateFunctionArn: !GetAtt EscalateFunction.Arn
      Policies:
        - LambdaInvokePolicy: { FunctionName: !Ref DenyFunction }
        - LambdaInvokePolicy: { FunctionName: !Ref AppealFunction }
        - LambdaInvokePolicy: { FunctionName: !Ref ReReviewFunction }
        - LambdaInvokePolicy: { FunctionName: !Ref FinalRulingFunction }
        - LambdaInvokePolicy: { FunctionName: !Ref ResolveFunction }
        - LambdaInvokePolicy: { FunctionName: !Ref EscalateFunction }

  # TODO(C): SiteBucket (private) + CloudFront distribution w/ OAC for the React SPA
  # TODO(C): AWS::Budgets::Budget at $5/$10 -> SNS -> EscalationEmail
  # TODO(C): SSM Parameter Store entries for per-agent model ids / feature flags

Outputs:
  ApiBaseUrl:
    Description: HTTP API base URL (frontend talks to <this>/api/...)
    Value: !Sub "https://${HttpApi}.execute-api.${AWS::Region}.amazonaws.com/${StageName}"
  JobsTableName:
    Value: !Ref JobsTable
  StateMachineArn:
    Value: !Ref NegotiationStateMachine
